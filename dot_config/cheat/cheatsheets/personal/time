# 時刻関連, locale

- 時刻調整 date -s "10:15:00"
    https://www.cyberciti.biz/faq/howto-set-date-time-from-linux-command-prompt/
    localeの調整までは行わないので､日付は変更されない｡

# systemdの場合
timedatectl:
	現在の設定を表示
timedatectl list-timezones:
timedatectl set-timezone Asia/Tokyo
